// de — stub. Empty record means every key falls back to English.
// Translate keys here as needed; existing UI keeps working at any
// coverage level (partial translations OK).

import type { MessageKey } from "./en";

export const de: Partial<Record<MessageKey, string>> = {
  // Add translations here. Until then everything falls back to en.
};
